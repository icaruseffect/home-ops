---
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: contabo-pool
  namespace: metallb-system
spec:
  addresses:
    - "$(SECRET_PUBLIC_SUBNET_IP4)"
    - "$(SECRET_PUBLIC_SUBNET_IP6)"
  autoAssign: true
  avoidBuggyIPs: true
